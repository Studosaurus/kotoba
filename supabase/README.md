# Supabase

This directory is reserved for Supabase configuration, migrations, and seed data.

The MVP scaffold intentionally does not define database tables yet. Schema design should be introduced alongside the first implemented domain slice so persistence follows real product behavior rather than speculative modeling.

